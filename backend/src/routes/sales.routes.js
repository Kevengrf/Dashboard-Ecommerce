const express = require('express');
const router = express.Router();

// Ex: router.post('/leads', salesController.createLead); // Endpoint para n8n
// Ex: router.post('/deliveries', salesController.createDelivery);

module.exports = router;
